<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FluentDL.Views.SpectrogramDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentDL.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:FluentDL.Views"
    mc:Ignorable="d">
    <!--  Spectrogram dialog  -->
    <ContentDialog
        x:Name="Dialog"
        Title="View Spectrogram"
        CloseButtonText="Close"
        IsSecondaryButtonEnabled="True"
        SecondaryButtonClick="SpectrogramDialog_SecondaryButtonClick"
        SecondaryButtonText="Reset Zoom">
        <ContentDialog.Resources>
            <x:Double x:Key="ContentDialogMaxWidth">1500</x:Double>
            <x:Double x:Key="ContentDialogMaxHeight">1000</x:Double>
        </ContentDialog.Resources>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock
                x:Name="SpectrogramInfoText"
                Grid.Row="0"
                Margin="0,0,0,4"
                HorizontalAlignment="Center"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorPrimary}"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="" />
            <TextBlock
                Grid.Row="0"
                Margin="4,0,0,4"
                HorizontalAlignment="Left"
                Foreground="{ThemeResource TextFillColorSecondary}"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="Zoom: ctrl + scroll"
                Visibility="{Binding Visibility, ElementName=SpectrogramImage}" />

            <ScrollView
                x:Name="SpectrogramScrollView"
                Grid.Row="1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="Black"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                HorizontalScrollBarVisibility="Visible"
                HorizontalScrollMode="Auto"
                VerticalScrollBarVisibility="Visible"
                VerticalScrollMode="Auto"
                ZoomMode="Enabled">
                <Image
                    x:Name="SpectrogramImage"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Stretch="Uniform"
                    Visibility="Collapsed" />
            </ScrollView>
            <ProgressRing
                x:Name="SpectrogramProgressRing"
                Grid.Row="1"
                Margin="40" />
        </Grid>
    </ContentDialog>

</UserControl>
